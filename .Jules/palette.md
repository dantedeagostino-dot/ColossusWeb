## 2025-05-20 - Icon-only buttons missing accessible names
**Learning:** Icon-only buttons (like social media links and menu toggles) are visually clear but inaccessible to screen readers.
**Action:** Always verify icon-only buttons have an `aria-label` describing their function, especially in reusable components like cards.
